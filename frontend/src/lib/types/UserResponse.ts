export type UserDetails = {
    admin: boolean
    username: string
    first_name: string
    last_name: string
}

export type UserStore = {
    admin: boolean
    username: string
    password: string
    first_name: string
    last_name: string
}